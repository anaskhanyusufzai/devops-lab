name: DevOps CI pipeline

on:
    push:
        branches: [main]

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v3

            - name: Build Docker Image
              run: docker build -t devops-lab .



